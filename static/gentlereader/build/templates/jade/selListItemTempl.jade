section: a(href='/selections/#{model.get("slug")}')
  h3.selectioninlist-title #{model.get("title")}
  .selectioninlist-details
    span.selectioninlist-author #{model.get("author")}
    if model.get("from_display")
      | .&#32;!{model.get("from_display")}
    |&#32;(!{model.get("date_display")}). <span class="selectioninlist-posttime">Posted #{model.get("date_entered")}</span>

section.selectioninlist-teaser
  span.teaser-text.hyphenate!= model.get("teaser")
  a(href='/selections/#{model.get("slug")}').read-on read&nbsp;on