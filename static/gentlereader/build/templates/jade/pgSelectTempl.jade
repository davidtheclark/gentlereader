.x-adjustor(data-adjustor="page") &times;
.adjustor-h Pages
ol(start="0")#pagelist
  li.page-all(data-page='all').pagelist-i all #{itemCount} #{dataType}

  for p in pages
    li(data-page='#{p.num}').pagelist-i #{p.fill}
