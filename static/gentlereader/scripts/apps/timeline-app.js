// Generated by CoffeeScript 1.6.1

define(['jquery', "routers/browse-timeline-router", 'lib/unfinishedToggler'], function($, Router, unfinishedToggler) {
  var TimelineApp;
  TimelineApp = Backbone.View.extend({
    initialize: function() {
      $("#tl-list").unfinishedToggler({
        exclusive: false
      });
      this.router = new Router();
      if (Backbone.history.fragment) {
        return this.initialRouting();
      }
    },
    initialRouting: function() {
      var scrollTo;
      scrollTo = $(".tl-year[data-year='" + Backbone.history.fragment + "']").offset().top - 60;
      return $("html, body").animate({
        scrollTop: scrollTo
      });
    }
  });
  return TimelineApp;
});
